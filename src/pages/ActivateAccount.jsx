import AuthenticationView from '../authentication/AuthenticationView.jsx'

export default function ActivateAccountPage() {
  return <AuthenticationView mode="activate" onModeChange={() => {}} onAuthenticated={() => {}} />
}
